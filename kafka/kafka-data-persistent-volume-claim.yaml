apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kafka-data-volume-pv-claim
  namespace: kafka
spec:
  storageClassName: kafka-data
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi